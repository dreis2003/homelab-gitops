apiVersion: v1
kind: Service
metadata:
  name: ikon-brasil-web
  labels:
    app: ikon-brasil-web
spec:
  selector:
    app: ikon-brasil-web
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP