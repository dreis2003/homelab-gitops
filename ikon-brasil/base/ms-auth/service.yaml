apiVersion: v1
kind: Service
metadata:
  name: ms-auth
  labels:
    app: ms-auth
spec:
  selector:
    app: ms-auth
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP