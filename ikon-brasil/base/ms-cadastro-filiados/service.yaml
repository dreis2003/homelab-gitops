apiVersion: v1
kind: Service
metadata:
  name: ms-cadastro-filiados
  labels:
    app: ms-cadastro-filiados
spec:
  selector:
    app: ms-cadastro-filiados
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP